# Moltbot Environment Configuration Template
# Copy this file to /home/moltbot/.config/moltbot/.env and fill in your values
# Documentation: https://docs.molt.bot/concepts/models

# =============================================================================
# LLM Provider Configuration (choose one or more)
# =============================================================================

# Anthropic (Recommended - Claude Pro/Max with Opus 4.5)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI (Alternative)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini (Alternative)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# =============================================================================
# Gateway Configuration
# =============================================================================

# Port for the Gateway to listen on (default: 18789)
MOLTBOT_PORT=18789

# Gateway bind address (use 0.0.0.0 for external access, 127.0.0.1 for local only)
MOLTBOT_HOST=0.0.0.0

# =============================================================================
# Channel Configuration (uncomment and configure as needed)
# =============================================================================

# WhatsApp
# Follow setup guide: https://docs.molt.bot/channels/whatsapp
# WHATSAPP_ENABLED=true

# Telegram
# Get bot token from @BotFather: https://t.me/botfather
# TELEGRAM_BOT_TOKEN=

# Discord
# Create a bot at: https://discord.com/developers/applications
# DISCORD_BOT_TOKEN=

# Slack
# Create an app at: https://api.slack.com/apps
# SLACK_BOT_TOKEN=
# SLACK_APP_TOKEN=

# =============================================================================
# Security Configuration
# =============================================================================

# DM Policy: "pairing" (recommended) or "open"
# "pairing" requires approval for new contacts
# DM_POLICY=pairing

# Allowlist for DMs (comma-separated identifiers)
# Use phone numbers for WhatsApp, numeric chat IDs for Telegram, usernames for others.
# Use "*" to allow all (NOT recommended for security)
# DM_ALLOWLIST=

# =============================================================================
# Reverse Proxy / Trusted Proxies
# =============================================================================

# If you expose the Control UI through a reverse proxy (nginx, Caddy, Tailscale,
# etc.), set this to a comma-separated list of proxy IPs or CIDR ranges.
# The deploy script writes this value into gateway.trustedProxies so that the
# gateway reads the real client IP from X-Forwarded-For headers instead of
# treating every proxied request as a local client.
#
# Examples:
#   GATEWAY_TRUSTED_PROXIES=127.0.0.1          # proxy on the same machine
#   GATEWAY_TRUSTED_PROXIES=100.64.0.0/10      # Tailscale CGNAT range
#   GATEWAY_TRUSTED_PROXIES=127.0.0.1,::1      # localhost IPv4 + IPv6
#
# Leave unset (or empty) if you access the gateway directly without a proxy.
# GATEWAY_TRUSTED_PROXIES=

# =============================================================================
# Optional: Tailscale Configuration
# =============================================================================

# Enable Tailscale Serve/Funnel for secure remote access
# TAILSCALE_ENABLED=false
# TAILSCALE_MODE=serve  # "serve" for tailnet-only, "funnel" for public

# =============================================================================
# Optional: Node.js Performance Tuning
# =============================================================================

# Override the V8 heap size limit (in MB). The installer sets this
# automatically based on available RAM. Uncomment only to override.
# NODE_OPTIONS=--max-old-space-size=512

# =============================================================================
# Optional: Voice Configuration (ElevenLabs)
# =============================================================================

# ELEVENLABS_API_KEY=
